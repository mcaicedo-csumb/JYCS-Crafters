<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Header -->
    <TextView
        android:id="@+id/adminMessage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="You have admin privileges!"
        android:textSize="20sp"
        android:textStyle="bold"
        android:gravity="center"
        android:contentDescription="Admin Header"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="32dp"/>

    <!-- Row 1: Add / Remove -->
    <LinearLayout
        android:id="@+id/row1"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/adminMessage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp">

        <Button
            android:id="@+id/addUserButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:text="Add User"
            android:textAllCaps="false"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:contentDescription="Add User"/>

        <Button
            android:id="@+id/removeUserButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:text="Remove User"
            android:textAllCaps="false"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:contentDescription="Remove User"/>
    </LinearLayout>

    <!-- Row 2: Deactivate / Reactivate -->
    <LinearLayout
        android:id="@+id/row2"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/row1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp">

        <Button
            android:id="@+id/deactivateUserButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:text="Deactivate User"
            android:textAllCaps="false"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:contentDescription="Deactivate User"/>

        <Button
            android:id="@+id/reactivateUserButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:text="Reactivate User"
            android:textAllCaps="false"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:contentDescription="Reactivate User"/>
    </LinearLayout>

    <!-- Row 3: Delete Permanently / Change Password -->
    <LinearLayout
        android:id="@+id/row3"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/row2"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp">

        <Button
            android:id="@+id/deleteUserPermButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:text="Delete Permanently"
            android:textAllCaps="false"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:contentDescription="Delete User Permanently"/>

        <Button
            android:id="@+id/changePasswordButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:text="Change Password"
            android:textAllCaps="false"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:contentDescription="Change Password"/>
    </LinearLayout>

    <!-- Full-width Logout -->
    <Button
        android:id="@+id/logoutButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:minHeight="48dp"
        android:text="Logout"
        android:textAllCaps="false"
        android:contentDescription="Logout"
        app:layout_constraintTop_toBottomOf="@id/row3"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp"/>

    <!-- Users list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/userListRecycler"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/logoutButton"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>

