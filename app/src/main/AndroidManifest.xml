<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JYCSCrafters"
        tools:targetApi="31">
        <activity
            android:name=".FuelLogActivity"
            android:exported="false" />
        <activity
            android:name=".AddFuelEntryActivity"
            android:exported="false" />
        <activity
            android:name=".VehicleActivity"
            android:exported="false" />
        <activity
            android:name=".LandingPageActivity"
            android:exported="false" />
        <activity
            android:name=".AdminCheckActivity"
            android:exported="false" />

        <!-- Camila: admin tools (internal only) -->
        <activity
            android:name=".AdminLandingActivity"
            android:exported="false" />
        <activity
            android:name=".UserManagementActivity"
            android:exported="false" />
        <activity
            android:name=".VehicleReviewActivity"
            android:exported="false" />

        <!-- Not launcher; keep internal -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Launcher -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>